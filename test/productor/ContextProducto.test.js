/**
 * @jest-environment jsdom
 */
import { ProductosContextProvider } from "../../components/productor/ContextProducto.js";

jest.mock("axios");

describe("Fectch Data", () => {
  test("fetch data", async () => {
    
  });
});
